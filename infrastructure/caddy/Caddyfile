# Отдаёт собранный фронтенд и проксирует /api на сервис api.
:8080 {
	encode gzip

	handle /api/* {
		reverse_proxy api:3000
	}
	handle /docs* {
		reverse_proxy api:3000
	}
	handle /health {
		reverse_proxy api:3000
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
